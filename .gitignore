# ── 语法模型（420 MB，不进 git，每个部署目录各下一份，见 README）──────────────
# 注意：git reset --hard 不会动被忽略的未跟踪文件，所以 live 克隆更新时 .gram 不会丢。
# 但 git clean -xdf 会把它连同用户数据一起删掉 —— 永远不要在 live 克隆里跑那条命令。
*.gram

# ── 运行时生成的用户数据（红线：任何情况下不覆盖、不删除、不提交）─────────────
*.userdb/
user.yaml
installation.yaml
sync/

# ── librime 编译产物 ────────────────────────────────────────────────────────
build/

# ── 杂项 ───────────────────────────────────────────────────────────────────
.DS_Store
*private*
rime_console
rime_deployer
rime_patch
